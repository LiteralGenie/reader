edit chapter - 3
definitions page - 3
    dictionary jamo - 1
    dictionary lru cache - 1
add blocks and merge pages - 3
docker - 3
workers dying - 2
move worker init to outside web server (so that uvicorn can spawn multiple workers) - 2

demo
    resource blocks
        as panic button enabled by env
        ocr
        mtl
    resource limits
        length llm input
        length nlp input
        disable prefetch
        uploads per ip
    hide custom uploads
    lock default uploads
        no ocr edits
        no chapter edits
        no chapter uploads
        no series edits

use template for llm definitions
logging
create chapter from archive
random 404s

image load priority
fix spacing before nlp
hide low confidence matches
less verbose svelte proxying
diff sse progress updates
ocr progress doesnt update
mu / md icons
add chapter progress link-able
