series list
chapter list
dictionary page

move worker init to outside web server (so that uvicorn can spawn multiple workers)
display job status %
rescan option
logging
image load priority
fix spacing before nlp
scroll to word in dictionary view

sort definitions via llm