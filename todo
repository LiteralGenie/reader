series list
chapter list
definitions page
ocr crud
series crud

logging
move worker init to outside web server (so that uvicorn can spawn multiple workers)
display job status %
rescan option
logging
image load priority
fix spacing before nlp
scroll to word in dictionary view
centralize css styles
unload gpu memory when idle
